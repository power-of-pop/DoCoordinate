<div class="container px-5 px-sm-0">
<h2>Post details</h2>
  <div class="row">
    <div class="col-md-5 col-12 mb-5 text-center">
        <%= image_tag url_for(@post.post_image), class: "img-fluid w-75" %>
    </div>
    <div class="col-md-7 col-12">
      <table class='table'>
        <tbody>
          <tr>
            <% if @post.user %>
              <th class="border-bottom"><%= link_to @post.user.name, admin_user_path(@user) %></th>
            <% end %>
          </tr>
          <tr>
            <th class="border-bottom text-wrap text-break"><%= @post.title %></th>
          </tr>
          <tr>
            <th class="border-bottom text-wrap text-break"><%= simple_format(@post.body, sanitize: true) %></th>
          </tr>

          <tr>
            <td class="border-bottom text-wrap text-break"><%= @post.created_at.strftime('%Y/%m/%d %H:%M:%S') %></td>
          </tr>

          <tr>
            <th>
              <%= link_to "Delete", admin_post_path(@post), method: :delete, data: { confirm: "本当に消去しますか？" }, class: "btn btn-danger" %>
            </th>
          </tr>

        </tbody>
      </table>

      <!-- タグ表示 -->
      <div class="mb-3">
        <% @post.tags.each do |tag| %>
          <span class="badge bg-info text-white me-3"><%= tag.name %></span>
        <% end %>
      </div>

      <div>
        <%= @post.favorites.count %> いいね
        <p>コメント件数：<%= @post.post_comments.count %></p>
        <% @post.post_comments.each do |post_comment| %>
          <div class="row mb-2 border-bottom">
            <div class="col-2">
              <%= image_tag post_comment.user.get_profile_image(50,50) %><br>
              <%= link_to post_comment.user.name, admin_user_path(@user) %>
            </div>
            <div class="col-6">
              <%= simple_format(post_comment.comment) %>
              <%= post_comment.created_at.strftime('%Y/%m/%d %H:%M:%S') %>
            </div>
            <div>
              <%= link_to "delete", admin_post_post_comment_path(post_comment.post, post_comment), method: :delete, data: { confirm: "本当に消去しますか？" }, class: "btn btn-danger" %>
            </div>
          </div>
        <% end %>
      </div>
      <div>
        <%= link_to 'Back', admin_posts_path, class: "back" %>
      </div>
    </div>
  </div>
</div>
