<div class="container px-5 px-sm-0">
<h2>Post details</h2>
  <div class="row">
    <div class="col-5">
        <%= image_tag url_for(@post.post_image), size: "300x450" %>
    </div>
    <div class="col-7">
      <table class='table'>
        <tbody>
          <tr>
            <% if @post.user %>
              <th class="border-bottom"><%= link_to @post.user.name, user_path(@user) %></th>
            <% end %>
          </tr>
          <tr>
            <th class="border-bottom"><%= @post.title %></th>
          </tr>
          <tr>
            <th class="border-bottom"><%= @post.body %></th>
          </tr>
          <% if @post.user == current_user %>
          <tr>
            <th>
              <%= link_to "Edit post", edit_post_path(@post), class: "btn btn-success", style: "margin-right: 30px;" %>
              <%= link_to "Delete", post_path(@post), method: :delete, data: { confirm: "本当に消去しますか？" }, class: "btn btn-danger" %>
            </th>
          </tr>
          <% end %>
        </tbody>
      </table>
      <div>
        <%= link_to 'Back', posts_path, class: "back" %>
      </div>
    </div>
  </div>
</div>
